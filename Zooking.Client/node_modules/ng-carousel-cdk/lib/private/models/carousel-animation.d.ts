import { AnimationPlayer } from '@angular/animations';
import { Subscription } from 'rxjs';
/**
 * Animation state that is currently in process
 */
export declare class CarouselAnimation {
    from: number;
    to: number;
    player?: AnimationPlayer;
    onDoneSubscription$?: Subscription;
    startTime: number;
    constructor(from: number, to: number, player?: AnimationPlayer, onDoneSubscription$?: Subscription, startTime?: number);
}
