/**
 * Template context for carousel slide
 */
export declare class CarouselSlideContext {
    $implicit: any;
    itemIndex: number;
    isActive: boolean;
    inViewport: boolean;
    constructor($implicit: any, itemIndex: number, isActive: boolean, inViewport: boolean);
}
