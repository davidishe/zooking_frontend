import { EasingFunction } from 'bezier-easing';
/**
 * Calculates offset for animation specific tick
 */
export declare function animationOffsetSnapshot(currentPosition: number, totalDuration: number, from: number, to: number, offsetFallback: number, bezierFn: EasingFunction): number;
