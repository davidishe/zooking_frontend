import { CarouselAlignMode } from '../../../../carousel-align-mode';
import { CalculateOffsetResult } from './models/calculate-offset-result';
/**
 * Calculates offset by specified arguments as if current slide was centered
 */
export declare function calculateOffset(currentSlideIndex: number, alignMode: CarouselAlignMode, slideWidth: number, viewportWidth: number, slideQuantity: number, shouldLoop: boolean): CalculateOffsetResult;
