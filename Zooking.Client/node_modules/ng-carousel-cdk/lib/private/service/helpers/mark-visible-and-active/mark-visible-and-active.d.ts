import { CarouselAlignMode } from '../../../../carousel-align-mode';
import { CarouselSlide } from '../../../models/carousel-slide';
import { MarkVisibleAndActiveResult } from './models/mark-visible-and-active-result';
/**
 * Returns cloned slides with modified `inViewport` and `isActive` fields
 */
export declare function markVisibleAndActive(slides: CarouselSlide[], offset: number, slideWidth: number, viewportWidth: number, activeSlideIndex: number, threshold: number, alignMode: CarouselAlignMode): MarkVisibleAndActiveResult;
