import { CarouselSlide } from '../../../models/carousel-slide';
/**
 * Finds slide that is marked as `isActive` and returns its index
 */
export declare function findSlideIndex(slides: CarouselSlide[], newItemIndex: number, currentSlideIndex: number): number;
