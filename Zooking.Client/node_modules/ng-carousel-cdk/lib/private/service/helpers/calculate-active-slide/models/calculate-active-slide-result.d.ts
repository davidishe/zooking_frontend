/**
 * Result for helper function `calculateActiveSlide`
 */
export declare class CalculateActiveSlideResult {
    modifiedOffset: number;
    slideIndex: number;
    constructor(modifiedOffset?: number, slideIndex?: number);
}
