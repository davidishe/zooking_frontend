import { CarouselSlide } from '../../../../models/carousel-slide';
/**
 * Result for helper function `shuffleSlides`
 */
export declare class ShuffleSlidesResult {
    slides: CarouselSlide[];
    modifiedOffset: number;
    constructor(slides?: CarouselSlide[], modifiedOffset?: number);
}
