import { AnimationBuilder } from '@angular/animations';
import { CarouselWidthMode } from '../../../../carousel-width-mode';
import { CarouselAnimation } from '../../../models/carousel-animation';
export declare function startAnimation(container: HTMLElement, from: number, to: number, widthMode: CarouselWidthMode, transitionDuration: number, bezierArgs: number[], isBrowser: boolean, afterAnimationAction: () => void, animationBuilder: AnimationBuilder): CarouselAnimation;
