import { CarouselSlide } from '../../../../models/carousel-slide';
export declare class RemoveExcessiveResult {
    slides: CarouselSlide[];
    offset: number;
    constructor(slides: CarouselSlide[], offset: number);
}
